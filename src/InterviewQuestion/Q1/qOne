HashCode and Equal method

All the Wrapper class(Integer, Character, Double) has already implemented the hashcode() and equal() into it.
it already been overridden into all the wrapper classes.If two object hashcode is same at that time equal method
is going to be used and there the contents will be checked .
If the hashcode is same and content is same then that will be a duplicate element .And then that wil be rejected in the Map value


Java Recursion
